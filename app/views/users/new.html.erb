<div class="signup">
  <div class="container">
    <div class="form">

    <h1>Sign up</h1>
    <%= form_for(@user) do |f| %>
    <div class="field">
      <%= f.label :username %><br>
      <%= f.text_field(:username, {:class => 'form-control'}) %>
    </div>
    <div class="field">
      <%= f.label :email %><br>
      <%= f.text_field(:email, {:class => 'form-control'}) %>
    </div>
    <div class="field">
      <%= f.label :password %><br>
      <%= f.password_field(:password, {:class => 'form-control'}) %>
    </div>
    <div class="field">
      <%= f.hidden_field(:privilege, value: @privilege) %>
    </div>
    <div class="actions">
      <br>
      <%= f.submit("Create Account", {:class => 'btn btn-primary'}) %>
    </div>
    <% end %>

    </div>
  </div>
</div>
